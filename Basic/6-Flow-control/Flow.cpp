int main() {

    int a, b;

    // If
    if (a) { 
        // Do something...
    } else if (b == 1) {
        // Do something...
    } else {
        // Do something...
    }

    // Ternary operator
    a = (a == 0) ? 1 : 0;
    
    // Switch
    switch(a) {
        case 0:
            // Do something...
            break;
        case 1:
            // Do something...
            break;
        default:
            // Do something...    
    }

    // While loop
    while(a) {
        // Do something...
    }

    // Do/While loop
    do {
        // Do something...
    } while (a);

    for (int i = 0; i < 5; i++) {
        // Do something 5 times...
    }
    return 0;
}